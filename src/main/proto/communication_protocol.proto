syntax = "proto3";

package io.openfuture.chain.protocol;

message CommunicationProtocol {

  enum ServiceName {
    TIME_SYNCHRONIZATION = 0;
  }

  ServiceName service_name = 1;
  bytes service_payload = 2;
}
